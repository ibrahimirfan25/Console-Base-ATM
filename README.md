# Console-Base-ATM
this contains the main file which executes all the logic files that i have made and the two other files are checking that the csv file exsists or not and the account info checking file which takes input of account number and pin and then if correct it returns name and balance. As said in the task description
